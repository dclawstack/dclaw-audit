apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-audit.fullname" . }}
  labels:
    {{- include "dclaw-audit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dclaw-audit.selectorLabels" . | nindent 4 }}
