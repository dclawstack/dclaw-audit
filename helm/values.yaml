replicaCount: 1
image:
  backend:
    repository: dclaw-audit-backend
    tag: local-1779986882
    pullPolicy: Never
  frontend:
    repository: dclaw-audit-frontend
    tag: local-1779986882
    pullPolicy: Never
service:
  backend:
    type: ClusterIP
    port: 8037
  frontend:
    type: NodePort
    port: 3037
    nodePort: 30054
ingress:
  enabled: false
secret:
  data:
    DATABASE_URL: "postgresql+asyncpg://postgres:postgres@dclaw-shared-postgres:5432/dclaw_audit"
    APP_ENV: "production"
    SECRET_KEY: "dev-secret-change-me"
    OLLAMA_URL: "http://ollama:11434"
    OPENROUTER_API_KEY: ""
    NEXT_PUBLIC_API_URL: ""
    ENABLE_DEMO_MODE: "true"
resources:
  requests: { cpu: 10m, memory: 128Mi }
  limits:   { cpu: 300m, memory: 384Mi }
strategy:
  type: Recreate
